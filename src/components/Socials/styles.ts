import styled from "styled-components";

export const Container = styled.div`
    svg {
        color: #fff;
        cursor: pointer;
        margin: .25rem;
        transition: 500ms;
    
        &:hover {
            color: var(--dark-yellow);
        }
    }
`;